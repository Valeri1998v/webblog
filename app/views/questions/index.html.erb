<h2 class='title-lg mb'>Questions</h2>

<% @questions.each do |question| %>
    <p class='mb-sm'><%= link_to "Question #{question.id}", question_path(id: question.id) %></p>
<% end %>

<p class='mt-sm'><%= link_to "Create new question", new_question_path %></p>
